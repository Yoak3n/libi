# Pi Memory Sync

> Auto-generated from Pi persistent memory — 2026-06-30

## Global Memories

### User Profile

- **User**: yoa
- **OS**: Arch Linux
- **WM**: niri
- **Terminal**: kitty
- **Current Shell**: fish (after fish → bash → zsh+oh-my-zsh → fish)
- **Tools**: starship, zoxide, thefuck, eza, yazi, fastfetch
- **Custom Functions**: `f`/`waifu fastfetch`, `y`/`yazi`, `c`/`clear`, `pac`, `pacr`, `ls`→`eza`
- **Config Style**: self-contained single rc file, no reuse across switches
- **oh-my-zsh**: `~/.oh-my-zsh`, theme: random, plugins: git, extract, sudo, web-search, history, copyfile, dirhistory
- **Niri Fix**: Mod+T keybinding hardcoded `-e fish` — remove `-e fish` to let kitty use login shell; also used `kitty.conf shell /bin/fish`
- **Fastfetch Waifu**: bash script at `~/.config/scripts/fastfetch-random-wife.sh`, called from rc file
- **Shell Knowledge**: understands `$SHELL` (login shell via chsh) vs `$0` (running shell); interested in shell philosophy (compatibility vs breaking changes)

### Go Cross-Platform Tip

- For `Makefile` binaries, use `$(shell go env GOEXE)` to auto-detect `.exe` on Windows vs empty on Linux/macOS instead of hardcoding `.exe`

## Project Memories (bilibili)

*(No project-specific memories stored)*

## Failures / Lessons

*(No failure memories stored)*

---

*Sync timestamp: 2026-06-30*
